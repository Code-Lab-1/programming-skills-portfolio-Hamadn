# Importing the pi value from a library called math
from math import pi

# Assigning a variable(radius) to the prompt
radius = float(input("Enter the radius of the circle: "))

# Assigning a variable(area) to the formula of calculating the area of circle
area = pi*radius**2

# Printing the area of the circle
print(area)