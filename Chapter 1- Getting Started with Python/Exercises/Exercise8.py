# Assigning a variable(word) to the prompt
word = input('Enter word: ')

# Assigning a variable(size) to the property of length of word 
size = len(word)

# Printing the length of the entered word
print(size)