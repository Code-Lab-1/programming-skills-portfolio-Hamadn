# Assigning an integer value to a variable(a)
a = 2

# Runs a loop for numbers between the given range(1 - 10)
for x in range(1, 11):
# Multiplies the value of 'a' with every number in the range
    prod = a * x
# Prints the value of prod
    print(prod)