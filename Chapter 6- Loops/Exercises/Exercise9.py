# Assigning a variable(num) to an integer 
num = 75869
# Assigning a variable(count) to an integer
count = 0
# Checking until the value of num is not 0
while num != 0:
# Assigning a variable(num) to the formula
    num = num // 10
# Iterate the value of count and add 1
    count = count + 1
# Printing the string
print("Total digits are:", count)